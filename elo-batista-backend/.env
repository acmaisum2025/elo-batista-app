# elo-batista-backend/.env

# Porta em que o servidor Express será executado.
# Você pode mudar para outra porta se 5000 estiver em uso.
PORT=5000

# URL do seu frontend (para configurações de CORS mais restritivas em produção).
# Por enquanto, o CORS está configurado para permitir todas as origens,
# mas em produção, você pode querer restringir a apenas o seu frontend.
# Exemplo: FRONTEND_URL=http://localhost:3000 ou FRONTEND_URL=https://seu-app-frontend.com
# Por enquanto, não é estritamente necessário para o CORS que configuramos,
# mas é uma boa prática ter para futuras restrições.
# FRONTEND_URL=http://localhost:3000

# --- Informações do Firebase (Opcional, pois estamos usando serviceAccountKey.json) ---
# Geralmente, a chave de serviço JSON é a forma mais segura e recomendada
# para o Firebase Admin SDK. No entanto, se por algum motivo você precisar
# passar as credenciais via variáveis de ambiente (ex: em alguns ambientes de deploy
# que não permitem arquivos), você as colocaria aqui.
# Para o nosso setup atual, estas linhas são apenas para referência e não são usadas.

# FIREBASE_PROJECT_ID="seu-project-id-aqui"
# FIREBASE_CLIENT_EMAIL="seu-client-email-aqui"
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nSUA_CHAVE_PRIVADA_COMPLETA_AQUI\n-----END PRIVATE KEY-----\n"
# ATENÇÃO: A chave privada deve ser colocada entre aspas duplas e com '\n' para quebras de linha.
# Isso é MUITO sensível e deve ser tratado com extremo cuidado.